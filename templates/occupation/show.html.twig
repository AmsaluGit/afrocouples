 {% extends 'master/base.html.twig' %}
{#
{% block title %}Occupation{% endblock %}
 #}

{% block body %}
    <h1>Occupation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ occupation.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ occupation.name }}</td>
            </tr>
        </tbody>
    </table>
    <table>
         <tr>

         <td> <a href="{{ path('occupation_edit', {'id': occupation. id}) }}" class="btn  btn-primary">edit</a></td>
   <td>  <a href="{{ path('occupation_index') }}"  class="btn  btn-info">back to list</a></td>

   <td>  {{ include('occupation/_delete_form.html.twig') }} </td>

    </tr>

        
</table>

{% endblock %}
