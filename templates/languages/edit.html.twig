{% extends 'master/base.html.twig' %}
{# {% extends 'base.html.twig' %}

{% block title %}Edit Languages{% endblock %}
  #}

{% block body %}
    <h1>Edit Languages</h1>

    {{ include('languages/_form.html.twig', {'button_label': 'Update'}) }}
<hr>

<table>
        <tr>
        <td><a href="{{ path('languages_index') }}" class="btn  btn-info">back to list</a> </td>

        <td> {{ include('languages/_delete_form.html.twig') }} </td>

        </tr>
    </table>
{% endblock %}
 
       
   

