{# {% extends 'base.html.twig' %}

{% block title %}Nationality{% endblock %}
 #}

{% block body %}
    <h1>Nationality</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nationality.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ nationality.name }}</td>
            </tr>
        </tbody>
    </table>
    <table>
         <tr>
   <td>  <a href="{{ path('nationality_index') }}">back to list</a></td>

   <td> <a href="{{ path('nationality_edit', {'id': nationality. id}) }}">edit</a></td>

   <td>  {{ include('nationality/_delete_form.html.twig') }} </td>

    </tr>

        
</table>

{% endblock %}
