{# {% extends 'base.html.twig' %}

{% block title %}ContactUs{% endblock %}
 #}

{% block body %}
    <h1>ContactUs</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contact_u.id }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ contact_u.message }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ contact_u.email }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ contact_u.createdAt ? contact_u.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>
    <table>
         <tr>
   <td>  <a href="{{ path('contact_us_index') }}">back to list</a></td>

   <td> <a href="{{ path('contact_us_edit', {'id': contact_u. id}) }}">edit</a></td>

   <td>  {{ include('contact_us/_delete_form.html.twig') }} </td>

    </tr>

        
</table>

{% endblock %}
