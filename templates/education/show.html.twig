 {% extends 'master/base.html.twig' %}
{% block title %}Country{% endblock %}


{% block body %}
    <h1>Education</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ education.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ education.name }}</td>
            </tr>
        </tbody>
    </table>

    <table>
         <tr>
         <td><a href="{{ path('education_edit', {'id': education.id}) }}"  class="btn  btn-primary">edit</a></td>
         <td> <a href="{{ path('education_index') }}" class="btn  btn-info">back to list</a></td>
         <td> {{ include('education/_delete_form.html.twig') }}</a></td>

    </tr>

        
</table>

{% endblock %}
