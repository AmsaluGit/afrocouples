{% extends 'master/base.html.twig' %}
{# {% extends 'base.html.twig' %}

{% block title %}MaritalStatus{% endblock %}
 #}

{% block body %}
    <h3>MaritalStatus</h3>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ marital_status.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ marital_status.name }}</td>
            </tr>
        </tbody>
    </table>
    <table>
         <tr>

         <td> <a href="{{ path('marital_status_edit', {'id': marital_status. id}) }}" class="btn  btn-primary">edit</a></td>
   <td>  <a href="{{ path('marital_status_index') }}"  class="btn  btn-info">back to list</a></td>

   <td>  {{ include('marital_status/_delete_form.html.twig') }} </td>

    </tr>

        
</table>

{% endblock %}
