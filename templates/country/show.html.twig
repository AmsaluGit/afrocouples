{% extends 'master/base.html.twig' %}

{% block title %}Country{% endblock %}

{% block body %}
    <h1>Country</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ country.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ country.name }}</td>
            </tr>
        </tbody>
    </table>


    <table>
         <tr>
           
    <td><a href="{{ path('country_edit', {'id': country.id}) }}"  class="btn  btn-primary">edit</a></td>
    <td> <a href="{{ path('country_index') }}" class="btn  btn-info">back to list</a></td>
    <td> {{ include('country/_delete_form.html.twig') }}</a></td>
        </tr>

        
 </table>
    
{% endblock %}


