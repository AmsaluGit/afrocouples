{% extends 'master/base.html.twig' %}
{# {% extends 'base.html.twig' %}

{% block title %}Edit Education{% endblock %}
  #}

{% block body %}
    <h1>Edit Education</h1>

    {{ include('education/_form.html.twig', {'button_label': 'Update'}) }}
<hr>

<table>
        <tr>
        <td><a href="{{ path('education_index') }}" class="btn  btn-info">back to list</a> </td>

        <td> {{ include('education/_delete_form.html.twig') }} </td>

        </tr>
    </table>
{% endblock %}
 
       
   

