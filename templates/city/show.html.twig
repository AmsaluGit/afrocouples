{# {% extends 'base.html.twig' %}

{% block title %}City{% endblock %}
 #}

{% block body %}
    <h1>City</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ city.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ city.name }}</td>
            </tr>
        </tbody>
    </table>
    <table>
         <tr>
   <td>  <a href="{{ path('city_index') }}">back to list</a></td>

   <td> <a href="{{ path('city_edit', {'id': city. id}) }}">edit</a></td>

   <td>  {{ include('city/_delete_form.html.twig') }} </td>

    </tr>

        
</table>

{% endblock %}
