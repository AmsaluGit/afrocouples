{% extends 'master/base.html.twig' %}
{#
{% block title %}Languages{% endblock %}
 #}

{% block body %}
    <h1>Languages</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ language.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ language.name }}</td>
            </tr>
        </tbody>
    </table>
    <table>
         <tr>
   <td>  <a href="{{ path('languages_index') }}">back to list</a></td>

   <td> <a href="{{ path('languages_edit', {'id': language. id}) }}">edit</a></td>

   <td>  {{ include('languages/_delete_form.html.twig') }} </td>

    </tr>

        
</table>

{% endblock %}
